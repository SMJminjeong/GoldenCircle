server:
  port: 8000

spring:
  datasource:
    url: jdbc:oracle:thin:@minjeong_medium?TNS_ADMIN=/Users/songminjeong/Wallet/Wallet_minjeong
    username: admin
    password: Alskdjfhg12!
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    database: oracle
    database-platform: org.hibernate.dialect.OracleDialect
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace